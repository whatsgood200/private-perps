{
  "name": "private-perps",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "dev": "cd app && npm run dev",
    "build:frontend": "cd app && npm install --legacy-peer-deps && npm run build",
    "build:sdk": "cd sdk && npm install && npm run build"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.31.1",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.3.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@arcium-hq/client": "^0.9.7",
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^2.2.0",
    "@solana/spl-token": "^0.4.14",
    "@solana/web3.js": "^1.98.4",
    "bip39": "^3.1.0",
    "ed25519-hd-key": "^2.0.0"
  }
}
